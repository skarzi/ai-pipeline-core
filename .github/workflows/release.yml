name: Release

on:
  push:
    branches: [main]

jobs:
  check-version:
    runs-on: ubuntu-latest
    outputs:
      should_release: ${{ steps.detect.outputs.changed }}
      version: ${{ steps.read.outputs.version }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2  # Need current and previous commit for comparison

      - id: read
        name: Read version from pyproject.toml
        run: |
          VERSION=$(python - <<'PY'
          import tomllib,sys
          from pathlib import Path
          data=tomllib.loads(Path("pyproject.toml").read_text("utf-8"))
          print(data["project"]["version"])
          PY
          )
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - id: detect
        name: Detect if version changed in last commit
        run: |
          CURRENT_VERSION="${{ steps.read.outputs.version }}"
          TAG_EXISTS=$(git tag -l "v$CURRENT_VERSION" | wc -l)

          # If tag already exists, don't release
          if [ "$TAG_EXISTS" -gt 0 ]; then
            echo "Tag v$CURRENT_VERSION already exists, skipping release"
            echo "changed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # Check if there's a parent commit (not the first commit)
          if git rev-parse HEAD^ >/dev/null 2>&1; then
            # Has parent commit, check if version actually changed
            if git diff --name-only HEAD^..HEAD | grep -q "pyproject.toml"; then
              # pyproject.toml changed, now check if version changed
              PREV_VERSION=$(git show HEAD^:pyproject.toml | python -c "
          import tomllib, sys
          try:
              data = tomllib.load(sys.stdin.buffer)
              print(data['project']['version'])
          except (tomllib.TOMLDecodeError, KeyError):
              print('0.0.0')
          ")
              if [ "$CURRENT_VERSION" != "$PREV_VERSION" ]; then
                echo "Version changed from $PREV_VERSION to $CURRENT_VERSION"
                echo "changed=true" >> "$GITHUB_OUTPUT"
              else
                echo "Version unchanged at $CURRENT_VERSION"
                echo "changed=false" >> "$GITHUB_OUTPUT"
              fi
            else
              echo "pyproject.toml not changed"
              echo "changed=false" >> "$GITHUB_OUTPUT"
            fi
          else
            # First commit in repo, check if pyproject.toml exists
            if [ -f "pyproject.toml" ]; then
              echo "First commit with pyproject.toml"
              echo "changed=true" >> "$GITHUB_OUTPUT"
            else
              echo "changed=false" >> "$GITHUB_OUTPUT"
            fi
          fi

  quality-checks:
    needs: check-version
    if: needs.check-version.outputs.should_release == 'true'
    uses: ./.github/workflows/reusable-quality-checks.yml
    secrets: inherit  # Pass secrets for integration tests

  release:
    needs: [quality-checks, check-version]
    if: needs.check-version.outputs.should_release == 'true' && needs.quality-checks.result == 'success'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write  # For PyPI trusted publishing
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          python-version: '3.14'

      - name: Build
        run: uv build

      - name: Get previous tag
        id: previoustag
        uses: WyriHaximus/github-action-get-previous-tag@v1

      - name: Create GitHub Release
        uses: ncipollo/release-action@v1
        with:
          tag: v${{ needs.check-version.outputs.version }}
          name: Release ${{ needs.check-version.outputs.version }}
          body: |
            ## 🚀 Release ${{ needs.check-version.outputs.version }}

            This release was created automatically when the version was bumped.

            ### Install
            ```bash
            pip install ai-pipeline-core==${{ needs.check-version.outputs.version }}
            ```

            ### Changes
            Compare: https://github.com/bbarwik/ai-pipeline-core/compare/${{ steps.previoustag.outputs.tag }}...v${{ needs.check-version.outputs.version }}
          artifacts: dist/*

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          verbose: true
